{
  "name": "kev",
  "version": "0.0.0",
  "private": true,
  "description": "KEV — campus/exam app monorepo (Expo frontend + Spring Boot backend + Python ML service).",
  "license": "UNLICENSED",
  "workspaces": [
    "frontend",
    "packages/*"
  ],
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "packageManager": "npm@10.9.2",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:inline": "concurrently -k -n web,api,ml -c cyan,green,magenta \"npm:frontend\" \"npm:backend\" \"npm:ml\"",
    "services": "concurrently -k -n api,ml -c green,magenta \"npm:backend\" \"npm:ml\"",
    "frontend": "npm run start --workspace frontend",
    "backend": "node scripts/run-backend.mjs spring-boot:run",
    "backend:test": "node scripts/run-backend.mjs verify",
    "ml": "node scripts/run-ml.mjs dev",
    "ml:test": "node scripts/run-ml.mjs test",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "knip": "knip",
    "gen:api-types": "node scripts/gen-api-types.mjs",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,jsonc,md,mdx,yml,yaml,css}": [
      "prettier --write --ignore-unknown"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "@expo/config-plugins": "~54.0.4",
    "concurrently": "^9.1.2",
    "eslint": "^9.39.4",
    "husky": "^9.1.7",
    "knip": "^5.43.0",
    "lint-staged": "^15.3.0",
    "openapi-typescript": "^7.5.0",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "overrides": {
    "brace-expansion": "1.1.16",
    "js-yaml": "^4.1.0",
    "postcss": "^8.5.10",
    "uuid": "^11.1.1",
    "@expo/metro-config": {
      "postcss": "^8.5.10"
    }
  }
}
