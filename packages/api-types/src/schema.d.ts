/**
 * PLACEHOLDER — replaced by `npm run gen:api-types` (openapi-typescript reading
 * the backend's /v3/api-docs). These empty stubs let the workspace typecheck
 * before the backend has been generated against.
 */
export interface paths {}
export interface components {
  schemas: Record<string, never>;
}
export interface operations {}
